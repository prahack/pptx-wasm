{
  "$comment": [
    "Per-suite pixel tolerances against the LibreOffice oracle.",
    "",
    "`maxDiffRatio` is the fraction of pixels allowed to differ by more than",
    "`threshold` (pixelmatch's 0..1 perceptual distance). Every tolerance has a written",
    "reason. Widening one is a decision to record here, not a knob to turn quietly \u2014 if a",
    "diff grows, the first question is whether the viewer regressed, and the second is",
    "whether the oracle is the thing that is wrong.",
    "",
    "The oracle is LibreOffice, not PowerPoint. It antialiases differently, hints text",
    "differently, and rasterises effects differently, so a tolerance of zero is not",
    "achievable for anything with text in it. For the milestones where fidelity matters",
    "most (m2, m4), keep PowerPoint-exported references for manual spot checks."
  ],
  "defaults": {
    "threshold": 0.15,
    "maxDiffRatio": 0.02,
    "width": 960,
    "height": 540
  },
  "suites": [
    {
      "id": "m0",
      "fixture": "m0-blank.pptx",
      "maxDiffRatio": 0.001,
      "reason": "A blank slide is flat white. Anything above noise here is a real bug."
    },
    {
      "id": "m1",
      "fixture": "m1-basic.pptx",
      "maxDiffRatio": 0.02,
      "reason": "One text box and one rectangle. The rectangle should be near-exact; the tolerance is almost entirely the text's antialiasing and hinting."
    },
    {
      "id": "m2",
      "fixture": "m2-text.pptx",
      "maxDiffRatio": 0.06,
      "reason": "Text-heavy by design. LibreOffice's line breaking and glyph rasterisation differ from a browser's even when the wrap points agree, and a single word moving to another line lights up a large fraction of the slide. Watch the trend, not the absolute number; check wrap points via debugTrace when it moves."
    },
    {
      "id": "m3",
      "fixture": "m3-shapes.pptx",
      "maxDiffRatio": 0.035,
      "reason": "Preset geometry is generated by our own path builder rather than transcribed from the ECMA-376 tables, so curves on the more elaborate presets differ by a pixel or two along their edges. Fills and positions should be exact."
    },
    {
      "id": "m4",
      "fixture": "m4-template.pptx",
      "maxDiffRatio": 0.06,
      "reason": "The inheritance suite: every shape takes its position, size and formatting from the layout and master. Text dominates the diff, so the tolerance matches m2. A *positional* error would blow well past this, which is exactly what this suite is for."
    },
    {
      "id": "m5a",
      "fixture": "m5a-tables.pptx",
      "maxDiffRatio": 0.06,
      "reason": "Table borders land on half-pixel boundaries and LibreOffice snaps them differently, so every rule contributes a line of differing pixels. Cell text carries the rest."
    },
    {
      "id": "m5b",
      "fixture": "m5b-charts.pptx",
      "maxDiffRatio": 0.15,
      "reason": "Deliberately the loosest suite. chart.xml carries data and formatting but no geometry, so every renderer computes the plot itself: LibreOffice picks its own axis scale, gap widths, label placement and legend metrics, and none of those are specified by the file. The structural things this suite actually guards are that the right number of bars/wedges appear, in the right order, with distinct series colours, inside the frame \u2014 a regression in any of those blows well past this figure. Judge it by the trend and by looking at the diff, not by the number alone."
    },
    {
      "id": "m5c",
      "fixture": "m5c-effects.pptx",
      "maxDiffRatio": 0.02,
      "reason": "Gradients, alpha compositing and the outer shadow all render, and this suite now sits near 0.1%. The remaining budget is for the Gaussian blur radius, which LibreOffice and a browser compute differently \u2014 matching it exactly would mean matching its implementation. Note what this tolerance can and cannot catch: an effect vanishing entirely only moves the figure by about half a percent, so the real guard against that is the unit tests in layout::tests, which assert the SetShadow command and its scope directly. Pixel tolerance is the wrong instrument for 'did this feature stop working'."
    }
  ]
}
